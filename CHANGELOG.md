# Changelog

本项目所有重要变更都记录在此文件。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

- **队列 / 日志可拖动分隔条**：主界面队列与「该任务日志」之间新增可拖动分隔条（细线手柄、
  不可折叠），拖动即可改变两块区域的高度；初始比例与旧版一致，位置不做持久化。
- **「待密码」任务新增「重试」**：「运行日志 → 需要处理」卡片上，待密码任务现在有三个动作
  —— 「输入密码」「重试」「忽略」。「输入密码」改为**跳转到「密码本」标签页**（不再弹出旧窗口），
  并说明密码已在「固定密码本 / 临时密码 / 分享提取码」中查找过、可先去密码本保存再重试。
- **按目录删除策略**：监听目录新增「删除策略」，**仅在该卷没有可用回收站时**才出现（普通目录
  完全不受打扰）：**移入隔离区（推荐）/ 保留源文件 / 永久删除**。设置目录时按需询问一次，
  之后可在「目录设置」弹窗内随时修改。
- **隔离区**：特殊目录上选择「移入隔离区」后，源文件（含分卷）移动到
  `<输出目录或源目录>\_已删除\<日期>\`（同卷为秒级改名、跨卷安全拷贝，**绝不覆盖同名文件**）；
  「删除回溯」页显示**隔离区占用**，并可**还原**（搬回原位）或**彻底删除**。
  跨卷搬移失败时**保留原文件**，绝不静默销毁；隔离区目录全程不参与扫描。

### 修复

- **密码本表头排序三角压字**：排序指示器不再与「命中次数」文字重叠（顺带修掉深色主题下三角
  不可见的问题）；任务表 / 固定提取码表 / 删除回溯表的表头像素级未变。
- **拖入二维码图片不再受开关限制**：把二维码图片**拖进窗口**始终识别（显式手势=同意）；
  剪贴板识别仍受「二维码识别」开关控制，相关文案已同步澄清。
- **`_recover_finish` 静默退化**：跨目录分卷归拢完成后删除源文件的那条路径，此前因调用了一个
  已被移除的内部外壳而抛错并被外层 `except` 吞掉（文件被保留、未进回收站）；已改为直接调用，
  行为与主删除路径一致。

### 变更

- **未开实验性时，监听目录只扫表层**：不再有任何路径会深入子文件夹（此前有一条跨目录归拢的
  递归不受总开关约束，已按开关收紧）；顶层的跨目录分卷配对不受影响。
- **开实验性后，添加目录会拦截嵌套 / 重叠**：新增或修改监听目录时，若与已有目录相等、互为父目录
  或子目录会被拒绝并提示具体路径（按路径段判定，`C:\a\bc` 不会被误判为在 `C:\a\b` 内）；
  未开实验性时维持原有的「完全重复才跳过」。

### 内部

- 移除两处为迁就旧测试桩而引入的生产代码外壳（`_recycle_paths_policy`、`_fire_delete_hook`），
  改为直接调用；相关测试桩同步更新，断言未削弱。

### 测试

- 测试：**92 个离线测试文件全绿**（新增删除策略与隔离区引擎、隔离区界面、递归与重叠、
  待密码重试四个文件）。
- 测试一律**串行**运行：并行跑会因「真实 `config.json` 被独立写入者重写」产生假阳性。

## [1.2.0] - 2026-09-18

> **本轮重点**：界面从「单页 + 弹窗」改成**五标签工作台**（任务 / 日志 / 密码本 / 删除回溯 /
> 设置），三个此前只有「打开旧弹窗」按钮的页面变成**真页面**；分卷处理补上**跨名分卷链配对**
> 并**强制开启**；修掉一个会**无视你的设置回收源文件**的数据安全 bug；补齐 **DPI / 系统缩放适配**。
> 分享链路与百度清单仍属**实验性**，仍藏在「实验性功能」总开关之后。

### 新增

- **五标签工作台**：主界面改为标签页工作台，点标签即显示内容（不再需要"点按钮弹旧界面"）。
- **密码本真页面**：表格（口令 / 来源 / 命中次数 / 最近命中 / 备注）+ 实时搜索 + 四段筛选
  （全部 / 命中过 / 未命中 / 有来源）+ 口令掩码与「显示口令」切换 + 行内复制 / 编辑 / 删除
  （二次确认）+ 新增口令；`Ctrl+F` 聚焦搜索，表格焦点下 `Delete` 删除选中行。
  旧弹窗入口保留（「待密码任务 → 输入密码」仍可打开）。数据库已补 `note` 列，备注可编辑，
  行级增删改按行 id 生效（重名口令也能定向删除）；旧弹窗的整体保存不再清空备注。
- **固定提取码编辑回到密码本页**：页头新增模式分段「口令 | 固定提取码」。固定提取码表格
  （分享者UK / 提取码 / 需挑选 / 备注）支持行内增删改、单条新增，以及**批量文本编辑**
  （沿用 `分享者UK 提取码 [pick] [#备注]` 行格式）；页面同时补回旧弹窗的**排序 / 查重**。
- **删除回溯真页面**：表格（时间 / 文件名 / 大小 / 来源任务 / 原因）+ 搜索 + 两级筛选
  （全部 / 按原因 / 按日期）+ 行内「打开所在文件夹」「复制路径」+ **还原选中**（走系统回收站
  还原，仅「已删除」可用）+ 导出 CSV（导出当前可见记录）+ 清空记录（强二次确认）。
- **设置真页面**：覆盖全部 **38 个配置键**（监听目录 / 输出与移动 / 解压行为 / 分享与手势 /
  界面 / 常规 / 通知与实验性 / 二维码与剪贴板 / 网址信任 / 全局快捷键 / 托盘与关闭），
  每个控件标注**立即生效**或**需重启**（仅 `sevenzip_check_done` 需重启）；保存后回读校验，
  恢复默认需二次确认。
- **标签页快捷键**：`Ctrl+PgDn` / `Ctrl+PgUp` 循环切换标签页（浏览器惯例，末页→首页 / 首页→末页）。
- **跨名分卷链配对（强制开启）**：同一个压缩包被**改名上传**成多个分享时，按卷号把同目录里
  被改名的兄弟卷连成**整条链**（不止两卷）：**硬链接装配 + 7z + 口令本验证**通过后**整链改名**
  归一（日志同时留下旧 / 新绝对路径，可手动改回）。模糊候选、证据不足、目标名已存在一律
  **零动作**、只提示。该能力已并入基础解压逻辑、**强制开启**，`pair_split_enabled` 是唯一总闸。
- **有界延长等待**：已检测到改名链但验证未通过时，不再在 300 秒强解**截断的链**，改为延长到
  600 秒，到点再按现有分卷尝试，并如实提示「仍有缺失分卷，按现有分卷尝试（不会删除源文件）」。
- **产物时间戳校准**：可选把解压产物（含提升目录 / 移动目标 / 本次新建条目）的修改时间 stamps
  到当前时刻，`output_time_now`（默认开）。
- **任务历史上限**：`task_history_limit`（默认 500）控制历史记录保留量。
- **挑选窗：勾目录＝选中整棵子树**：文件挑选窗里勾选一个目录会**自动异步展开**它的子树并
  递归勾选（深度 4 / 节点 2000 / 并发 4 上限，触顶给出提示），取消则递归取消；**展开未完成
  时下载按钮禁用**并提示「正在展开目录…」，半个选择无法提交。
- **缺码小窗用完即关**：成功拉起客户端 / 挑选窗提交成功后，**属于同一分享**的提取码小窗
  自动关闭（不再等到 120 秒倒计时到点）；只关同一分享的窗，绝不误关别的分享。

### 修复与加固

- **`delete_source=False` 不再被无视（数据安全）**：此前只要走"提升"（promote）路径，源文件与
  全部分卷都会在"干净成功"后被回收，**完全无视你的删除开关**；现在开关为假时**保留源文件与
  分卷**（只做空目录原地清理），`mark_kept` 记录可达。失败路径依旧**绝不永久删除**。
- **退出期崩溃**：`style` 里挂的 app 级 Python 事件过滤器在解释器终结阶段仍被 Qt 回调，导致
  退出时 `0xC0000005` 访问违例（真页控件树更大 + 运行期换主题时必现）；现在退出前摘除。
- **分享手势（`Alt+2` / `Alt+3`）**：按下**当场**从剪贴板取分享链接与提取码（不再等抓 URL 的
  8 秒之后才写记录）；等待改为**意图驱动**（`share_gesture_wait_sec`，默认 60 秒、钳制 5..600），
  超时或失败**只提示、绝不回退到旧链接**；提取码与分享链接**同源**（修掉 `errno=-9`
  「提取码校验失败」）；整条链路单次拉起 + 总超时 90 秒 + 忙标志看门狗。
- **日志页 7 项**：重载后"已复制"链接复活；主窗隐藏时双通知且承诺一个弹不出的窗口；切换主题
  不重绘已画出的日志行；复制失败恢复到错误的行；"已复制 / 复制失败"同屏；分享记录行时间戳不一致。
- **DPI / 系统缩放适配**：HiDPI 相关属性在**任何 Qt 对象创建之前**设置（此前顺序不对会整段
  失效）；缩放策略用 `PassThrough`；窗口与最小尺寸按可用区收敛（100%–200% × 两种分辨率共
  10 组矩阵，全部无越界、无可见裁切）。
- **状态栏失败药丸被垂直裁切 10px**（20px 定高 vs 样式要求的 30px）修复。
- **界面细节**：筛选 chip 省略化（长目录名不再撑破）、弹窗滑入方向改为从主窗右缘出入。
- **`xxx.7z.001` 这种命名会永久等待（分卷）**：带独立末卷的命名风格下，「缺末卷」分支会在
  跨名配对检查与 300 秒兜底**之前**就返回，导致被改名上传的包**既不配对、也不兜底**，只能
  人工改名。现在两条分支共用一个「链检查 + 有界延长 + 兜底」实现，语义统一。
- **改名兄弟卷在下载时不推平等待计时器（分卷）**：首卷这一组只认同名兄弟，被改名的兄弟卷
  既不算到齐、也不算「本组在下载」、更不推平计时器 —— 极端情况下首卷会**白等满 300 秒**
  再强解一次（本项目最初的原始事故正是如此：尾卷落盘 9 秒后，程序仍等满 300 秒）。现在
  「同目录里存在任何未完成下载 / 任何正在长大的文件」都会推平计时器（**不限文件名**），
  并设 **30 分钟连续活动上限**，到点仍按现有分卷诚实兜底。
- **截断分卷链的无限重试**：一次截断链失败后会**每约 300 秒重试一次且永不放弃**。现在最多
  **3 次**、间隔阶梯 **300 → 900 → 1800 秒**，之后放弃并给出一条日志 + 一条通知，**源文件与
  分卷原地保留**；文件身份或目录内容一变就重新计数（新卷到达自动复活）。
- **解压时的悬浮进度条小窗**：为兼容旧代码保留的「进度条垫子」没有父窗口，被 `show()` 后
  变成一个独立顶层小窗。垫子已整体移除，进度仍显示在底栏与目录胶囊上。
- **主窗隐藏时"承诺一个弹不出的窗"**：剪贴板快路径在托盘状态下会先发一条「请在右侧小窗
  填写提取码」（承诺一个不会出现的窗）；现在统一走诚实文案（给出**三种方式**：打开主界面
  即会弹出小窗 / `Alt+2` 用临时码 / 把「链接 + 提取码」整段复制到剪贴板），且一次动作只发
  一条气泡。
- **密码本不再遮蔽口令**：口令改为默认直接显示原文，并移除「显示口令/隐藏口令」按钮与编辑框里
  的同名复选框（该机制整体删除）。
- **拖放路径接上跨名分卷链配对**：与监听路径共用同一套「链检查 + 7z 验证 + 改名兄弟卷」
  （验证通过才改名），拖入被改名的首卷不再只拼出截断载荷。
- **命中次数与最近命中从来没真正计数过**：唯一的计数点被「是否用密码字典解压」这个开关挡住，
  而 GUI 的两处解压入口都把该开关关着 —— 计数点成了**死代码**，命中次数恒为 0、最近命中恒为空。
  现在「成功用到的口令」一律计入统计（写入只影响 `used_count` / `last_used_at`，字典内容仍只在
  开字典时作为候选，关字典的运行行为完全不变）；最近命中列显示最近一次命中的时间。
- **命中/最近命中不实时**：页面只在切页或手动刷新时才重读数据库。现在页面在可见期间每 2 秒做一次
  轻量校验（数据无变化就不重建，有变化才刷新并保留选中行与滚动位置），命中后几秒内自动更新。
- **密码本排序改为点表头（原「排序」按钮删除）**：旧按钮是"重写数据库顺序"的实现，副作用是**把所有
  口令的创建时间冲成同一时刻**，并且会连带改变解压尝试顺序。现在点列头按该列升/降序（表头带 ▼/▲
  指示），**纯显示层、绝不改写数据库**；默认按**命中次数降序**（命中越多排越前）。
- **来源=剪贴板的临时口令行删不掉**：临时口令行此前被「仅长期口令可编辑」的判断挡住，删除按钮是
  禁用状态且没有接信号。现在剪贴板（临时）行可以单独删除（`state.remove_temp_password` 同锁清理
  密码列表与时间戳并落盘）；长期口令仍按 id 精确删除。
- **新增口令窗口支持「批量导入」**：同一窗口内切换为批量模式（一行一个口令的大文本框），备注一次
  填写、附加到该批每一条；空行与批内重复会被跳过，并给出「导入 N、跳过 M」的诚实统计。
- **多选（Shift / Ctrl / Shift+Ctrl，不加复选框）**：删除回溯的「还原选中」现在能一次还原多条（先确认，
  再给「成功 X / 失败 Y / 跳过 Z」的诚实回执）；密码本在选中 **≥2 条**时，表头上方出现「复制选中」
  （按当前排序从上到下一行一个写入剪贴板）与「删除选中」（先弹警告再删），选中 ≤1 条或常态下不显示。
- **表头与单元格不对齐**：来源 / 命中次数 等列的表头文字对齐与单元格不一致，且表头内边距（10px）
  与单元格（6px）不同。现在每列表头对齐跟随该列单元格（数字列右对齐、文本列左对齐）并统一内边距，
  删除回溯页同样处理；顺带修掉最近命中列因字体度量不一致被省略号截断的问题。
- **任务队列不实时**：队列只在切页 / 换筛选 / 点刷新时才重读数据库，任务运行期间坐在队列页看不到
  任何变化；而且**拖放解压的文件压根不建任务行**，永远不会出现在队列里。现在任务生命周期的每个
  落库点都会投递一条轻量事件，界面以 250ms **合并**刷新一次（避免逐个事件重建整表的抖动），刷新
  时保留选中行与滚动位置，**拖入的压缩包也会立刻出现在队列中**。
- **设置页**：① 新增**左侧分区目录**（点一下跳到对应分区）—— 此前页面约 6 屏高却完全没有索引，
  而旧版设置弹窗本来就有此目录，新版把它弄丢了；② 删除「监听目录」分区（已集成在页面头部的目录
  条，重复）与「输出与移动」分区（"统一指定所有监听目录的输出目录"这个全局覆盖项会与每个目录的
  个性化设置冲突）；每个目录自己的输出目录 / 删除源文件仍可在头部目录条里单独设置；③ 分区重排为
  「解压行为 → 常规 → 界面 → 分享与手势 → 二维码与剪贴板 → 通知与实验性 → 网址信任 → 全局快捷键
  → 托盘与关闭」；④ 修复**所有分区标题下那行说明文字被裁切**（左边缘吃掉首个字的偏旁、顶部吃掉
  最上一横）：根因是标签分配高度用的是 `fontMetrics().height()`（11px）而绘制按 `lineSpacing()`
  （13px），且左右零余量 —— 现按行高补足并留 1px 左右余量，125% 缩放下逐标签实测上下各有 3px 余量。

- **拖入二维码图片（新增入口）**：拖入图片文件会走**与剪贴板图片完全相同**的二维码链路
  （md5 去重 → 解码 → URL 信任 / 打开浏览器 / 剪贴板联动）。前置过滤很便宜：先看扩展名，
  再读前 32 字节魔数，超过 16MB 直接跳过（不为明显的大文件白跑解码）；**伪装成图片扩展名的
  压缩包仍按压缩包解压**（回落既有归档路径）。此路径不建任务行、跑在短命线程里不卡界面。
- **命中次数不再记「假命中」**：此前只要解压成功且候选口令非空，就会把**候选列表里的第一个
  口令**记成一次命中 —— 连**未加密**的压缩包也会中招，密码本的命中统计因此严重虚高（本机密码
  本有 62 条口令，等于每次解压未加密包都会给第一条刷一次）。现在引擎如实返回「是否真的用到
  口令解锁了加密内容」，只有真命中才计数。
- **分卷链放弃后不再留下「永远排队」的任务行**：自动重试（3 次 / 300-900-1800 秒）用尽后会
  留下一条永远 `queued`、且任务页无法重试也无法清除的僵尸行。现在改为终态并给出诚实原因
  （源文件与分卷原样保留，可通过「重试」手动再试）。
- **同一文件不会被并发解压两次**：拖放路径新增「在飞」去重（按真实路径 + 大小 + 修改时间），
  重复拖入或连点「重试」只会记一条日志；此前会起两个线程同时写同一个输出目录，并互相覆盖
  任务行的终态。
- **拖放任务也能看到「该任务日志」**：拖放路径此前没有设置线程级日志上下文，任务详情里的
  该任务日志是空的；现在与监听路径同一套语义（异常路径也会清理上下文）。
- **二维码失败原因如实**：图片打开失败 / 解码失败 / 解码进程异常退出此前被混为一谈（判定用的
  标记串与实际输出不匹配），现在三者能区分并如实告知。
- **解压失败的提示不再塞整段 7-Zip 英文输出**：失败通知与队列错误列此前会把 7-Zip 的 banner、
  帮助与错误行整段塞进去（气泡根本显示不下）。现在面向用户的是简短中文原因（「分卷链不完整
  （缺少兄弟分卷）」「密码错误」「不是压缩包或已损坏」等），**原始输出仍完整保留在日志里**。
- **回溯记录改为原子写**：`deletion_trail.json` 此前是「就地截断写」，崩溃 / 断电正好落在写入
  中间会把**全部还原记录**毁掉（还原能力与「已处理」判定一起失效）。现在与配置、临时口令一致
  走「写同目录临时文件 + `os.replace`」，并容忍损坏文件优雅降级。
- **配置改动不会再被静默丢弃**：保存配置此前先释放了锁，后台线程并发改配置会让序列化抛
  `RuntimeError` 并被 `except: pass` 吞掉 —— 改动没了且毫无提示。现在在锁内取一致快照再序列化。
- **数据库建表脚本每进程只跑一次**：此前**每次开连接**（包括每写一条日志）都会重跑整段
  `CREATE TABLE/INDEX` 加两条注定失败的 `ALTER TABLE`，是全 app 最大的性能开销来源。
- **短时 7-Zip 元数据调用补上超时**：此前没有超时，挂住的 7z 会让监听线程永久卡死。同时修掉
  口令传递的一个错误写法（同时传 `input=` 与 `stdin=`，只要带口令就必然抛 `ValueError` 并被吞，
  导致头加密压缩包永远拿不到体积进度）。
- **解压产物不再静默覆盖同名文件**：输出目录里已存在同名文件时会改用不冲突的名字并记日志；
  此前 `shutil.move` 会直接把你的文件覆盖掉。
- **任务表与提取码表的表头/内容对齐**：这两张表的表头此前缺少对齐角色（单元格右对齐数字、
  表头却居中），现在与密码本、删除回溯一致；分卷「整体成功」的判定也从「只比大小」改为
  「按路径+大小」比对，避免把残缺解压误判为成功。
- **密码本与任务页的刷新更省**：密码本的 2 秒实时刷新此前在**页面从未显示**时也在查库，现在
  只在可见期间运行；任务表刷新加了「行指纹短路」，解压期间不再每次事件都重建整表控件；口令列
  补了 tooltip。watcher 的已见文件表改为有界 LRU，长会话内存不再单调增长。
- **快捷键注册失败时如实标注**：配置了全局快捷键但注册失败（例如被别的程序占用）时，状态栏
  此前照样显示「快捷键 Alt+1」，按了没反应也没有原因；现在会标注「未生效」。

### 其它——老配置里残留的这个键会被**无害丢弃**，配对行为
  不再受它影响；② `delete_source=False` 现在**真的生效**：关掉它，源文件与分卷会留在原处
  （属行为变化，请按需设置）；③ 界面首次以新工作台形态出现，旧弹窗入口基本保留；
  ④ `passwords` 表新增 `note` 列（首次启动自动补列，幂等、不动旧行）。
- **更正 1.1.3 的一句话**：1.1.3 的更新日志写「主窗藏在托盘时也能弹出」小窗，与实现不符——
  实现一直是「主窗隐藏 / 最小化时**只发一条诚实气泡、不弹独立小窗**」，避免"凭空"冒出顶层
  窗口打扰；1.2.0 起这条气泡明确给出**三种填写方式**。
- 测试：87 个离线测试文件全绿（新增链配对、拖放路径配对、分卷等待语义、重试上限、分享手势、时间戳、
  DPI 几何、数据安全闸门、三个新页面、固定提取码、挑选窗连带子树、小窗自动关闭、标签页快捷键、
  命中计数与页面实时刷新、表头排序（含「排序不改写数据库」的数据安全断言）、临时口令删除、批量
  导入、多选与「复制选中 / 删除选中」、删除回溯多项还原、队列实时与合并刷新、拖放建任务行、
  设置页目录与说明行裁切回归、**二维码拖入（含伪装归档回落与大小阈值）**、**命中统计真伪门控**、
  **分卷放弃落终态**、**拖放并发去重**、**原子写 / 配置持锁 / 建表一次 / 已见表有界**、
  **run_silent 口令传递与超时**、**同名文件不覆盖**、**失败文案简短化**、**表头对齐**等专项）。
  测试一律**串行**运行：并行跑会因「真实 `config.json` 被独立写入者重写」产生假阳性。

## [1.1.3] - 2026-09-18

> **实验性说明**：本轮新增「**填提取码小窗**」（右侧非阻塞、120 秒倒计时）并把 2.F 分享
> 链路的手势体验补齐（感知缺口与绑定时机）。整条链路**仍属实验性**，仍藏在「实验性
> 功能」总开关之后。此外修掉两处会让文件**永久静默失效**的观察层 bug。

### 新增

- **填提取码小窗（右侧、非阻塞、120 秒）**：分享链接**没有提取码**时（链接自带码就不
  打扰）在主屏右侧弹出小窗：4 位输入框（已知码会自动预填）、逐秒倒计时、两个按钮
  「本次使用（`Alt+2`）」与「绑定并下载（`Alt+3`）」。到点自动关闭并丢弃框内内容，
  **不会**擅自拿框里的码发起任何操作。主窗藏在托盘时也能弹出。
- **小窗里填的码优先级最高**：手动填的码 > 链接自带/文本内嵌（权威） > 120 秒内最新
  复制的码。顺带修掉一个"明明复制了新码却仍用旧码"的取值 bug：抓取阶段从历史里猜出来
  的码不再被**黏死**在记录上，手势按下时会重新取最新候选。
- **`Alt+3` = 把该分享者的固定提取码绑定下来**：**拉起成功后**才写入映射（失败则只本次
  生效并提示）；此后该分享者的分享可直接用固定码下载。无需再手写密码本里的
  `uk code [pick] [#备注]` 行（该格式仍保留用于批量编辑）。`Alt+2` 保持"只用本次"。
- **二维码解码期间的"预定任务"**：手势按下时若"还没有分享链接记录"且**二维码正在解码**，
  不再只提示"复制一下分享链接即可"——改为登记一次性预定任务，解码出百度分享链接后
  **自动替你补按一次**（`Alt+2` / `Alt+3` 各记各的；60 秒未等到自动作废）。
- **日志里的网址可点一次即静默复制**：日志中的 `http(s)` 链接标蓝可点，点击**只复制到
  剪贴板**（**绝不**触发网址信任、二维码抓取、临时密码捕获或分享记录），复制后该链接
  **降级为普通文本**、不再可点——方便之后整段框选复制日志。链接在点击那一刻现算，
  日志被 3000 行上限裁剪也不会错位。

### 修复与加固

- **长下载 / 重新下载不再被永久静默跳过**（核心）：观察层原先只给"暂时不像压缩包"的
  新文件 4 轮（约 8 秒）复查机会，其后文件名留在"已看过"集合里**再也不看**——下载超过
  8 秒、或下到一半被替换的文件因此永远不处理。现在"已看过"记录**带身份**（大小 + 修改
  时间）：身份变化即静默重走检查，观察期内身份持续变化则**重置观察期**；同时对"持续增长
  却始终不是压缩包"的文件加了 churn 上限（约 2 分钟）与"大小稳定后再给一次机会"的兜底。
- **翻译 JSON 监控不再死循环**：原先"5 分钟超时放弃"会删掉窗口记录、同一轮又立刻把同一
  个 JSON 当新发现重新登记（计时归零）→ 每 5 分钟无限循环。现在放弃时**按文件身份记住**，
  文件不变就不再重试（也不重复打日志），文件一变立刻恢复监控。
- **百度清单（实验性）批次不再张冠李戴**：批次原先取 `server_path` 的根目录名，而
  「我的资源」是客户端给**所有**分享下载用的通用根，导致不同分享者的文件被并成一批、
  播报出误导性的「N 项全部完成」。现在**按分享划分**（`shareid` + `share_uk`），显示名
  跳过通用根取分享层目录（如 `我的资源/<分享目录>/…` → `<分享目录>`），播报措辞改为「**本批次**
  N 项全部完成」。
- **监听根不再被误判成"程序目录"**：只要监听目录里有一个 `.exe`/`.py`（工具目录常混装），
  旧规则就会把**整个监听根**判成程序目录 → 直接落在根下的下载一律被"百度清单模式"跳过。
  现在**被监听目录自身永不算程序目录**；其它目录要求"≥2 个程序文件 且 目录内没有正在
  下载的包"才算，真程序目录依旧受保护。
- **静默复制失败不再吞掉你的手动复制**：日志链接复制失败时不再推进剪贴板去重状态，
  此后你真正手动复制同一内容不会被静默忽略。
- **mtime 单独变化不再触发整包重解压**：观察层重走判据改为只看**大小**变化，避免索引器
  / 杀软只碰修改时间就让大包被重新解压一遍。

### 其它

- README 重写：以基础能力为主，实验性部分只作一句提示（不稳定、仍在修），并补上
  `libzbar-64.dll` / `libiconv.dll` 的放置说明。

## [1.1.2] - 2026-09-17

> **实验性说明**：本轮给 2.F 分享链路补上「**失效链接识别**」与「**空码闸门**」，并把
> 捕获路径的接线断点修好。整条链路**仍属实验性**，仍藏在「实验性功能」总开关之后。

### 新增

- **失效分享链接识别（分享被取消 / 过期 / 违规无法访问 / 不存在）**：链路第一步就会
  读取分享页，命中服务器直出的失效标记即判定「链接已失效」并**立刻短路**——不再进入
  `verify`、不消耗那唯一一次试探、绝不唤起客户端。自动（剪贴板）路径**抓取即判定**
  （零额外请求，且不再发出 `share_link` 事件）；**手动手势**（托盘 / `Alt+2` / `Alt+3`）
  对已判定失效的链接**直接弹 Windows 托盘通知**「分享链接已失效」，不盯日志、常驻托盘
  的人也能立刻知道。判据只用服务器直出的信号（`<title>` 失效文案族、
  `share_page_type:"error"`），**刻意不用** `neglect`、空的 `error-reason` 与 `errno`
  数字——这些在正常分享页上也可能出现，会误杀。
- **空码闸门**：新版分享的提取码是**强制必填**，空提取码提交必然失败。现在 `pwd` 为空时
  在失效判定之后、`tplconfig` / `verify` **之前**就短路返回「该分享需要提取码」，交回
  上层提示补码——不再发注定失败的请求、不再白烧唯一一次 verify 配额。（失效判定刻意
  排在它前面：裸链接也能先拿到准确的「已失效」结论，而不是误报成缺码。）
- **补码回捞改用 120 秒严格时效**：剪贴板回捞提取码改用 `fresh_code_from_history`
  （只认最近 120 秒内复制过的内容），避免拿旧码去 `verify` 白烧配额。

### 修复与加固

- **修复 2.F 接线断点**：`baidu_task` 漏转发 `mapped_code` / `fresh_code_from_history` 等
  名字，导致剪贴板捕获路径**静默降级**（「该分享者配有固定提取码」永远判不出来、回捞
  补码永不生效——异常被 `except` 吞掉，不报错、不写日志）。现已补上，并**刻意不**转发
  无时效的 `recent_code_from_history`，防止以后又拿旧码去试探。
- **修复分享页 `share_uk` / `shareid` 解析**：真实页面里这对键是**带引号**的 JSON 写法，
  且与 `yunData` 里的变量引用写法并存；原先只认后者，导致解析失败（外在表现就是
  「该分享者未配置固定提取码」）。
- **修复「链接 + 提取码」整段文本被当成 URL**：抓取与记录改用干净 URL、提取码仍从原文
  提取（原先脏 URL 会作为 Referer 带中文而导致请求失败）。
- **空码探测不再误报**：自动路径原先把「任何准备失败」都当成「需要提取码」；现在
  「已失效」与「其它失败」分流——失效链接不会再弹「请输入提取码」，而你按下手势遇到
  失败时也不再一片安静（失效→专门通知；其它失败→托盘提示）。
- `tplconfig` 缺字段时给出明确中文原因（原先抛 `KeyError` 被兜成英文 `"'data'"`）。
- **手势缺提取码时给可操作提示**：新版分享提取码必填，手动拉起若拿不到码，现在**不发
  网络请求、也不累加拉起计数**（原先会白跑一次页面请求，并让之后真正成功的那次被
  「重复拉起需确认」误挡），直接提示「分享缺少提取码」；配固定码的分享者则提示改用
  「固定提取码」手势。
- **收紧剪贴板「临时密码」误捕**：含版本号样式（如 `… 1.30`）或 4 个以上空白分词的
  **标题 / 句子**不再被当成密码捕获（原先会把 `Some Show Title 1.30` 这类串记进
  临时密码，开着「自动加入长期密码本」时还会污染密码本）；4 位提取码与普通多词口令
  **不受影响**，设置里的「智能过滤」子项仍可放松这条判定。
- **拉起通知不再等复核轮询**：唤醒客户端**一发出就通知**「已请求客户端下载」（原先
  要等 `/api/invoker/check` 复核轮询跑完才通知，常见情况白等满 12 秒）；
  复核降级为**静默兜底**、最多 3 次（上限 4.5 秒），只有 `errno≠0` 时才补一条
  「拉起后校验失败」的纠正通知。挑选/子集那条链路（`分享下载`）同样受益。
- **手势后看门狗提速 30 秒**：分享拉起手势后，任务库轮询即使在空闲期也临时按
  active 间隔跑，客户端接单的新任务更快被读到（更快进入批次预登记），与手势日志
  落在同一时间尺度上；不影响原有的空闲/活跃切换与失败退避。

### 其它

- **启动日志瘦身**：实验性任务库的启动输出由约 24 行降到 **2 行**（「未完成任务」告警
  保留），批次 / 分卷组 / 条目明细移到设置页「立即读取网盘任务库」按钮里；界面日志框
  加 3000 行上限。另修掉探测线程与轮询线程各打一遍「任务库选中」的重复行。
- **设置页拆出独立「实验性」分区**：实验性总开关与两个分享快捷键集中一处，「全局快捷
  键」页只留主热键；「网址信任」页补一行说明（分享链路不走网址信任判定）。
- `.gitignore` 补齐：SQLite 随身文件（`toolbox.db-wal/-shm/-journal`，里面是同样的明文
  数据）、轮转的崩溃日志、工具缓存目录与打包 / 虚拟环境产物。

## [1.1.1] - 2026-09-15

> **实验性说明**：本轮把分享链路（2.F）的 d1–d9 全部补齐，**仍属实验性**，整条链路
> 依旧藏在「设置 → 常规 → 实验性功能」总开关之后。**该链路不携带浏览器登录态、
> 也不使用你的浏览器 cookie**：若网盘客户端未在运行时被唤起，冷启动出来的客户端
> 可能处于未登录状态，需要重新登录。为规避这一点，本轮加了「登录态闸门」——
> **客户端未运行时直接跳过投递**（只记日志提示你手动打开客户端）。

### 新增

- **分享内容挑选下载**：`/share/list` 的结果现在能映射到程序内的「选择要下载的文件」
  窗口——目录**懒加载展开**（点开哪层才列哪层，复用同一次会话、不重复 verify），
  勾选后**只提交勾中的文件**（支持任意层级，可只下某个子目录里的某个文件），
  同一链接可**多次挑选**不同批次。分享者映射行新增「需要挑选」标记（`pick`）；
  另加全局开关「分享下载前先让我挑选文件（实验性）」（默认关）。
- **缺提取码回退（d3/d4/d5）**：URL 的 `?pwd=` → 文本内嵌提取码 → **就近剪贴板历史**
  （带时间戳、取绝对最近；只试 1 个）。分享者的**固定提取码映射不再自动套用**，
  必须显式手势：非模态询问面板 / 托盘菜单项 / 可选全局快捷键 `hotkey_share_code`。
  询问面板 **3 分钟无操作自动按「忽略本次」** 处理。
- **特殊分享者固定提取码（`share_code_map`）**：密码本对话框新增编辑区，行格式
  `分享者UK 提取码 [pick] [#备注]`；老库启动时自动 `ALTER TABLE` 补 `pick` 列。
- **会话内重复拉起需确认（d7）**：同一 `surl` 在本次运行内第二次起，拉起前必须经你
  同意（隐藏到托盘时不弹窗、只记日志 + 托盘提示）。

### 修复与加固

- **监控线程拆分（d8）**：剪贴板轮询线程只做「检测 + 入队」，网络 / 子进程 / 浏览器
  I/O 全部交给单个 worker 线程串行执行。轮询单次停顿由最长 8~20 秒降到毫秒级，
  「就近找码」依赖的剪贴板历史不再被卡丢；历史条目改为带时间戳并加锁，队列满时
  改为**淘汰最旧**（保住你最新的操作，避免刚复制的链接被静默丢弃）。
- **登录态闸门**：`commit_download` 提交前检查网盘客户端进程，**未运行则不投递唤醒**，
  避免无登录态冷启动导致需要重新登录。
- **防验证码（§4）**：单次分享最多 1 次 `/share/verify`、失败即停不重试、两次 verify
  间隔 ≥1s、识别到图形验证码立即放弃并发出一条提示。
- 二维码解出的分享链接与文本链接**汇入同一链路、受同一开关**（d1/d2）；链接识别
  收紧为**只认 `pan.baidu.com`**（d6），其它域名回退为原来的浏览器打开。
- 挑选窗**取消 / 关闭 / Esc 也会唤醒等待中的线程**（原先会卡住 30 分钟并拒绝后续拉起）；
  主界面隐藏到托盘时不弹挑选窗。
- 设置页「实验性功能」区补一条常驻警告说明上述登录态影响；本进程内**首次自动拉起**
  时额外提醒一次。

## [1.1.0] - 2026-09-14

> **实验性说明**：本轮新增的「百度网盘分享链接 → 拉起客户端下载」全链路（2.F）
> **仍属实验性功能**，尚未广泛测试。**整条链路都藏在「设置 → 常规 → 实验性功能」
> 总开关之后**：未开启时不捕获分享链接、托盘菜单不显示该项、分享快捷键不生效；
> 开启后「自动拉起」仍需再单独打开（默认关），手动拉起（托盘菜单 / 可选快捷键）
> 也要你显式触发，不会自己乱下。

### 新增

- **「用客户端下载最近分享」可选全局快捷键**：设置页「全局快捷键」新增第二行
  `hotkey_share`（留空 = 不设置，**默认不设置**），设好后即使主界面在托盘里也能
  一键把最近捕获的分享链接交给网盘客户端下载（等价托盘菜单那项）。与主快捷键
  各自独立注册（`HOTKEY_ID_SHARE`），分享热键注册失败不重试、不打扰。
- **监听路径新增「模式」：表层（安全）/ 百度清单（含子目录）**。表层模式即原有行为
  （只处理监听目录表面一层的文件）；百度清单模式会**额外**按百度网盘任务清单，处理
  下载到子目录里的压缩包与分卷（含归档内部穿透），**清单外的路径一律不碰**。
  两种模式都不新增监听路径，从结构上避免「同目录被两个监听器抢文件」。
  - 安全兜底：未开启实验性功能 / 客户端未运行 / 历史被清 → 自动只走表层模式；
  - 只处理「任务已完成」或「此前确实处理过（粘性记忆，存 `toolbox.db`）」的路径；
  - 文件大小需连续稳定 6 秒、且不是 `.baiduyun.p.downloading` 才处理；
  - **疑似程序目录保护**：同目录含 `.exe/.dll/.bat/...` 时只记一条日志、不自动解压；
  - 「解压到」目录与监听根重叠时提示一次，避免自我循环。
- 主界面「网盘下载目录」按钮：识别到的百度下载目录会以**百度清单模式**加入监听。
- **实验性：网盘任务跟踪（下载完成即触发 / 批次预登记 / 未完成提示 / 重复检测）**：
  - **批次预登记**：下载一开始就按「分享根」归批，输出整批期望文件清单（含套层的
    `local_path`），为提前建目录、提前判断分卷齐全提供权威输入；
  - **下载完成即触发**：任务从 `download_file` 消失且能在下载历史中确认时判定完成；
    分卷组全部完成且文件落盘后，`monitors` 的「等分卷」逻辑会**提前放行**解压
    （只加速、不阻断：拿不到信息一律回退原有磁盘启发式）；
  - **未完成任务提示**：启动时若 `download_file` 非空，通知「有 N 个网盘任务未完成
    （或仍在下载）」；
  - **重复下载检测**：新任务若与历史记录「同 `server_path` + 同 `size`」，可选提示。
- 通知页新增「网盘任务（实验性）」分组与三个独立开关（完成 / 未完成 / 重复）。
- **网址信任按用途拆成两套（互不影响）**：「自动在浏览器打开」（二维码解出的链接）
  与「下载识别二维码」（复制的网址被拉取、判断是否二维码图片）此前共用同一套
  `new_domain_action` + 白/黑名单，导致「同一个域名既想自动打开、又不想被扫描」
  无法两全（加黑名单会连带禁掉自动打开）。现 `url_trust` 拆为 `open` / `fetch`
  两份独立配置，各有自己的新域名默认行为与白/黑名单；内置敏感地址拦截仍两用途共享。
  设置页「网址信任」相应拆成两个分区；询问弹窗的「永久信任 / 永久拒绝」按当前用途
  写入对应名单。**旧版扁平配置启动时自动迁移到两边**，行为不变。
  （例：网盘分享链接不可能是二维码，可把 `drive.uc.cn` 放入 `fetch` 黑名单、
  同时留在 `open` 白名单里自动打开。）
- **实验性（2.F）：分享「链接 ↔ 客户端下载任务」关联打通（链接→提取码→下载→解压）**。
  客户端分享下载时，本地库活动表 `download_file.download_url` / `param2` 里带
  `shareid` / `share_uk` / `fs_id` / `md5` / `sekey`（只在**下载进行中**才有值，完成进
  历史表就没了）；而分享页 `https://pan.baidu.com/s/<surl>` **公开可读、无需登录**，
  `window.yunData` 里有同组 `shareid` / `share_uk`。现在：
  - `baidu_db` 活动任务查询新增读 `download_url` / `param2` / `error_code` / `status_changetime`；
  - `baidu_manifest` 新增纯解析 `parse_share_download()`（从 download_url/param2 取
    shareid/share_uk/fs_id/md5/sekey）、`extract_share_ids_from_html()`（从分享页 HTML 取
    shareid/share_uk）、`parse_share_url()`、`remember_share_link()`、`share_link_for()`；
  - 剪贴板出现百度分享链接时（`qr_url_enabled`）顺手抓页并记住 `surl → shareid`；
  - `observe_tasks` 给活动任务挂 `share` 关联键，新任务日志据此输出
    「分享 shareid=… uk=… fs_id=…，链接 …」。
  于是「链接 ↔ 下载任务」可用 `shareid` 精确对齐（兜底：`server_path`+`size`+`md5`）。
  - 另新增 `baidu_share.invoke_download()`：完整复刻「分享链接 → tplconfig 取 sign →
    verify 校验提取码得 sekey → share/list 列文件 → sharedownload 转存（拿到不透明的
    `filelist` 字符串令牌）→ invoker/get + online + send 投递 → 用
    `baiduyunguanjia://evoked-download/?browserId=…&seq=…` 唤起客户端 → invoker/check
    轮询确认」这条链路，即可**用客户端下载该分享（"拉起"下载）**——不下载、不登录、
    不碰网页，绕开 IDM。
    - **整包下载**：`/share/list` 分页（每页 50，去重保序）收齐分享根目录下**全部**
      条目，一次性提交 `fid_list`/`path_list`；服务端只回一个不透明的 `list` 令牌，
      该令牌已覆盖全部条目，客户端据此**全下**（实测 2 文件分享 → 2 个文件全部落盘）。
    - **「自动拉起」实验性开关（默认关）**：设置页「实验性功能」新增
      「检测到分享链接时自动拉起客户端下载」。开启后，剪贴板捕获到分享链接即自动
      触发整包下载（后台线程执行约 20s 的轮询，不阻塞界面；同一时刻只允许一个，
      重复触发会跳过）；默认关闭，仍可用托盘菜单「用客户端打开最近分享」手动触发。

### 修复

- **点托盘「用客户端打开最近分享」后整个程序卡死**：该托盘动作在 **Qt UI 线程**上
  同步调用了 `invoke_download()`（含约 20s 的 `/api/invoker/check` 轮询），把事件
  循环整个冻住，托盘提示也要等它跑完才出现。现把手动、自动两条路径统一到
  `_start_share_invoke()`：**一律后台 daemon 线程**执行，托盘提示改走 `hub.q`
  队列由主线程消费（绝不从工作线程碰 UI），`_share_invoke_busy` 忙标志防重复。
- **设置窗口「网址信任」页内容过长，且稍微拖动窗口高度会突变**：一是该页太高，
  低分辨率屏不友好；二是窗口高度会被反复"自适应"，拖动时被最小高度弹回。
  现**每个设置页都装入滚动区**（内容过高就滚动），并且高度**只在首次自适应一次**
  （上限降到 640、最小 420），之后用户拖动多少就是多少，不再回弹。
- **复制百度分享链接被「网址信任」拦下，导致不记录、不拉起**：分享链接此前搭在
  「复制网址→下载识别二维码」这条链路里，位于 `url_trust.fetch` 的信任判定**之后**，
  于是未信任的 `pan.baidu.com` 会被判为「已阻止访问未信任的网址」而直接返回，
  分享链接既不记录也不触发 2.F 拉起。现把分享链接拆成**独立入口**
  （`monitors._handle_baidu_share()`），**先于**网址信任判定执行：命中即抓公开分享页
  （无需登录）记录 `shareid/share_uk` 并投递 `share_link` 事件，再交主窗口按
  `baidu_auto_invoke` 决定是否自动拉起；非分享链接仍走原有二维码链路，不受影响。

- **「设置 → 常规 → 主题」下拉的展开方式三套主题不一致**（浅色向下带动画、跟随
  系统向下瞬时、深色向上瞬时）：根因是 QComboBox 弹出列表的**方向 / 动画 /
  默认委托**都由同一个 style hint `SH_ComboBox_Popup` 决定——浅色 `windowsvista`
  返回 0（下拉式：在组合框下方展开 + 150ms 滚动动画），深色 `Fusion` 返回 1
  （菜单式：相对当前项定位、可能向上弹、且不播放动画）。现用 `QProxyStyle` 把
  深色样式的该 hint 也归零，三套主题统一为「在下方展开 + 展开动画」。
- **浅色主题下主题下拉展开后高度塌陷**：切换到「浅色」后再次点开主题下拉，弹出的
  选项列表只剩约一行字高，三个选项挤在一起、文字被裁；而「跟随系统 / 深色」两项
  正常。根因是下拉列表的默认委托 `QComboBoxDelegate`（继承 `QItemDelegate`）
  **不识别 QSS 的 `QComboBox QAbstractItemView::item` 行高规则**，行高于是退回
  字高（约 13px）。现：给弹出项补上 `padding` 行高规则，并在控件 polish / show 时
  把下拉列表委托统一换成 `QStyledItemDelegate`（应用级事件过滤器，可覆盖运行期才
  创建的对话框控件）；配合上一条把两套样式都归一为下拉式，两套主题的弹出高度与
  行高保持一致（浅色约 23px/行、深色 21px/行）。
- **深色主题下勾选框 / 单选框看不见**：深色下复选框的方框、单选框的圆圈几乎与
  背景同色（只剩对勾 / 圆点悬空可见）。根因是样式表给 `QCheckBox / QRadioButton`
  设了 `background: transparent`，`QStyleSheetStyle` 会连带把绘制指示器所用调色板
  的 `Base` 变成透明 / 黑，Fusion 便用「≈背景」的颜色画框；浅色用原生
  `windowsvista` 绘制指示器，不受影响。现：把 `QCheckBox / QRadioButton` 从
  transparent 规则中移除，给「未选中」态补一条主题配色的可见描边；「已选中」态
  改为**主题主色填充 + 白色对勾 / 圆点**，一并解决「选中态反而不如未选中醒目」。
- **所有主题下组合框都没有下拉箭头**：只要用 QSS 定制了 `QComboBox::drop-down`
  （本样式表为了扁平外观设了 `border: none; width: 22px`），Qt 就不再自动绘制
  `::down-arrow`——浅色 / 深色都一样；而 QSS 的 `::down-arrow` 只认 `image:`
  （CSS 三角写法在子控件上不生效）。现：运行时用 QPainter 生成主题配色的 V 形箭头
  PNG，再以 `url()` 引回样式表，既保留扁平外观，箭头也随主题取色。
- **运行时生成的主题图标散落在数据目录**：下拉箭头 / 选中对勾 / 单选圆点的 PNG
  原先直接写在数据目录（且以 `.` 开头），现统一移入 `cache/` 子目录（`paths.CACHE_DIR`），
  并把 `.gitignore` 由「逐个文件名」改为忽略整个 `cache/`。
- **实验性「百度清单模式」：同一文件被重新下载后不会被解压**。根因在任务跟踪状态机
  `observe_tasks()`：它只在**首次**看到某个 `local_path` 时记录 `task_id`；重新下载
  同一路径时复用了旧条目却不刷新 `task_id`，于是新任务结束时用旧 `task_id` 去匹配
  「消失的任务」永远匹配不上，该条目的状态就**永远停在 `active`**——而 `_baidu_poll`
  只处理 `state=="done"` 的条目，于是永不解压。现：① 重新下载（同路径再次出现）时
  刷新 `task_id` / `server_path` / `size` 等字段；② 完成判定不再依赖相邻两拍的差集，
  改为「该条目是否仍在当前 `download_file` 活动集合里」，与 `task_id` 是否陈旧无关。
- **下载目录推断在历史只有一条记录时把「根」推成文件路径**：`detect_download_root()`
  用 `os.path.commonpath()` 求公共前缀，单条记录会直接返回该文件本身，使「下载根」
  变成一个文件路径（影响主界面「网盘下载目录」按钮与日志提示）。现改为对**文件所在
  目录**求公共前缀，保证结果一定是目录。
- 开启实验性功能、但识别出的百度下载目录**未加入监听**时，日志补一句提示
  「该目录仅监控、不解压」，避免误以为「开了实验性 = 来料会被自动处理」
  （监听范围始终是显式白名单）。
- **百度清单模式：解压产物被抬升到监听根，破坏下载目录结构**。`_handle()` 里
  `promote_to` 默认取的是**监听路径根**，解压成功后把顶层文件夹「抬升」到根目录；
  表层模式下源文件本就在根、看不出来，但百度清单模式处理的是下载到**子目录**里的
  包，于是产物跑到根目录、原位留下空文件夹，也断开了「文件取自哪个下载目录」的关系。
  现默认改为**源文件所在目录**（表层模式下二者等价，子目录场景则保留原结构）。
- **实验性「百度清单模式」：同一套分卷被下载到不同子目录时无法解压**。有些分享把
  一套分卷分散在多个子目录（`E:\test\假如说\1\test.7z.001`、`…\2\test.7z.002`），而
  7-Zip 只在同一目录里找兄弟卷——既有到齐判定（`_volume_ready` / `volume_hint` /
  `_stage_rar_volumes`）都是**目录局部**的，于是首卷一直判「未到齐」，等到超时才硬试、
  7-Zip 报缺卷，反复失败。现：清单侧新增 `gather_volume_set()`（同批次 + `is_volume_file`
  口径识别同套分卷、**卷号重复即判定歧义并放弃**），`_baidu_poll` 在首卷为跨目录集合
  且清单显示全部下完时，先把散落的兄弟分卷**归拢**到首卷目录（同盘 `shutil.move` 即
  瞬间改名，冲突/失败则回滚），再交回既有目录局部管线——到齐判断、解压、源文件回收
  （含分卷）与删除回溯全部复用，产物落在首卷目录。只凑清单内同批次同系列的分卷，
  清单外一律不碰。
- **剪贴板网址识别：把「链接 + 提取码」整段当网址访问，报 `URL can't contain control
  characters`**。复制分享文本（如 `https://drive.uc.cn/s/650826bca4d74 码：KKu7`）时，
  「网址形式的二维码图片识别」（`qr_url_enabled`）会把整段文本直接交给 urllib 发请求，
  其中的空格/中文触发 `URL can't contain control characters. '/s/650826... 码：KKu7'`
  而被跳过（日志「网址访问失败（跳过）」）。现在 `_extract_url()` 按 RFC 3986 的 ASCII
  URL 字符集，在**首个非法字符（空格/中文/全角标点）处截断**再返回，`_maybe_process_url()`
  只访问截出的真正网址；顺带修好了「网址后紧跟中文、中间无空格」的变体。

- **跨来源分卷：同一套分卷被拆进多个源文件时，既解不出、又被误报「完成」**。上传者把
  一套分卷切开塞进两个不同的伪装 `.mp4`，程序按源文件各自解到各自目录：末卷 `xxx.zip`
  报 `Missing volume : xxx.z01`、首卷 `xxx.z01` 单独留下，两半永不碰面。两个毛病：
  ① `extract.py` 判「嵌套缺卷」只看 `is_volume_name`（`.NNN`/`.zNN`/`.partN.rar`），
  而末卷 `base.zip`/`base.rar` 不带编号 → 落进「损坏可跳过」分支，保留失败文件后**整体
  仍报成功**（假成功）；② 到齐判断只查同一目录。现 `extract.py` 新增
  `is_split_gap_error()`（`"Missing volume"` 本身即判缺卷），缺卷时**整体判失败**并保留
  锚点分卷（`keep_output_dir`，失败回退不再清掉分卷，且不再把已就位的分卷误改名为
  `_failed`）；`monitors.py` 新增跨目录归拢：在监听范围（监听根 + 输出目录）内按系列名
  找回兄弟分卷、搬到锚点旁重试，等待期间不重复解压（`_split_pending` + 10s 节流复查），
  凑齐即恢复并回收已消费的源文件；超时（30 分钟）放弃且**分卷与源文件都保留**，可手动处理。

## [1.0.3] - 2026-09-12

### 新增

- **实验性功能开关（默认关闭）**：`设置 → 常规 → 「开启实验性功能」`。开启后，
  程序会以**只读**方式探测百度网盘客户端的本地任务库
  （`…\users\*\BaiduYunGuanjia.db`），并在启动时向运行日志输出「下载批次 /
  分卷组」汇总，用于验证按批次还原目录结构与分卷配对。安全约束：只读打开
  （`mode=ro`）+ 短连接（打开→查询→立即关闭）+ 本端 `busy_timeout` + 异常全部
  回退；不写库、不改 journal_mode、不 VACUUM、不持长事务，不影响正在运行的
  网盘客户端；且**绝不接触**运行中的 WAL 库 `BaiduYunCacheFileV0.db`。
- **实验性：百度网盘下载目录识别**：主界面新增「网盘下载目录」按钮（需先开启
  实验性功能）——只读任务库的历史 `local_path` 推断下载根目录（本机为 `E:\test`）
  并一键加入监听路径；已在监听中则提示。
- **实验性：活动下载任务轮询**：后台低频（5s）只读 `download_file`，下载开始/结束
  时在运行日志输出活动任务清单（目标 `local_path`、大小），用于验证"下载即预登记"
  （文件落盘前就掌握批次/结构/分卷）。

### 修复

- **无扩展名压缩包解压后源文件不删、残留 `*_extracted` 中间目录**：当压缩包
  没有扩展名（如 `E:\test\2022`）且其内层顶层文件夹同名（`2022`）时，提升逻辑
  的目标恰好是源文件本身；`_dirs_conflict` 把「目标是文件」误判为无冲突，进而
  尝试把文件夹「合并」进一个文件路径而抛异常，异常被后处理静默吞掉，导致
  **源文件与输出目录都没被回收**。现：合并前先确认目标是目录；若占位的正是本次
  要回收的源文件/其分卷，则先回收让出位置，提升后文件夹用回原名（不再退化成
  `名称(1)`），且删除回调只触发一次（回收/永久删除合并上报）。
- **设置页首次打开被压扁/文字被裁**：设置窗口初始尺寸写死 `640×520`，
  加了「网址信任」第 4 个单选项等之后，长页高度超出，导致首次打开控件被压缩、
  文字看不清，要拖动窗口才撑开。现改为按内容自适应初始尺寸，并把最小高度抬到
  内容下限（`minimumSizeHint`），长页首屏即完整显示。
- **长选项文案过长**：解压/网址信任/二维码与剪贴板/通知/常规/密码本等处描述过长的
  选项文案一律缩短，原详细说明移入**悬停提示**（与「临时密码」两项一致），
  避免低分辨率或默认宽度下文字被截断。

## [1.0.2] - 2026-09-11

### 新增

- **二维码内容分类处理**：解码出的文本按内容类型区分处理（纯扫描二维码
  与「复制链接→识别二维码」两条路径统一复用同一套逻辑）
  - 无链接的普通内容 / 单个二维码出现多个链接 → 写回剪贴板；`code` 设置下
    内容含提取码则只抬升提取码
  - 纯链接 → 信任判定后打开浏览器，按设置做剪贴板联动（含提取码抬升）
  - 链接夹杂其他内容 → 截取链接打开（走黑白名单，阻止时内容仍写剪贴板）
    + 按设置写剪贴板：`code` 且文本内含提取码时**抬升该提取码**
    （如「…链接 提取码：Zdjn」→ 剪贴板只留 `Zdjn`），否则写回全部内容
- **临时密码自动清理**：临时密码除「本次开机内有效」外，再叠加**有效期**
  （默认 24 小时）与**保留条数上限**（默认 200 条），超时/超量自动淘汰；
  两项均可在设置里自行调整。修复 Windows「快速启动」下每天关机不重置开机
  计时、导致临时密码长期堆积的问题。
- **临时密码捕获过滤（「设置 → 二维码与剪贴板 → 临时密码」）**：两级开关——
  - 「网址排除」（父，宽松，默认开）：带 :// 的网址不记为临时密码
  - 「智能过滤」（子，更严格，默认开；缩进、父关则禁用）：在网址排除基础上，
    再排除多行文本、文件路径/UNC、带常见扩展名的文件名、含句读标点的句子
  - 父开关关闭 → **照单全收**（连网址也收）
  - 有效期 / 保留上限同处该分组
- **密码本窗口**：长期密码区与临时密码区改为可拖动分隔条，可自行调节两区高度。
- **通知开关补全（「设置 → 通知」）**：新增 3 个**托盘提示**独立开关——「已最小化
  到托盘」「程序已在运行，已打开主界面」「有新的网址等待确认」。此前这三类托盘
  气泡只受通知总开关控制，现可各自单独关闭（总开关关闭时一并禁用）。
- **网址信任「无操作」策略（「设置 → 网址信任」）**：遇到未信任的新域名时新增
  「无操作」选项并**设为默认**——命中的新公网域名不打开、不询问、也不记录，
  静默跳过（适合只想手动白名单放行的用户）。

### 修复

- **「自动信任并打开 / 自动拒绝」不写名单**：`decide_host` 在 `auto_whitelist` /
  `auto_blacklist` 下只返回放行/拒绝，从不把域名写入白名单/黑名单，导致选「自动
  信任并打开」后白名单始终为空（同一域名每次都被当成新域名）。现命中 auto 策略
  时把新公网域名持久化进对应名单（弹窗点「信任 / 永久拒绝」的路径本来就正确）。
- **二维码文本乱码**：二维码解码子进程（`qr_worker`）的 stdout 在管道下默认
  用系统编码（中文 Windows 为 GBK）输出，父进程却按 UTF-8 解码，导致二维码里的
  中文（含「提取码」字样）变成乱码，只有 URL、提取码等 ASCII 幸存（所以链接仍能
  正常打开）。现强制子进程以 UTF-8 输出，与父进程解码对齐，混合内容里的提取码
  才能被正确识别并按设置抬升。
- **分卷「批次括号」命名**（如外层 `…part01(2).rar` → 中层 `…part01(1).rar`
  → 内层 `…part01.rar`，基础名相同）：修正分卷兄弟判定、首卷识别与嵌套层
  规范化暂存，避免把不同批次的卷误当兄弟而误删下一层源卷，或首卷不入队
  导致嵌套文件被清理丢失。
- **0 字节幽灵文件**：文件占用检测不再用会「创建文件」的 `open(path,"ab")`
  探测（文件已被消费时会原地重建 0 字节空文件）；`is_archive_file` 对不
  存在的文件返回 False。
- **进程内全局状态**：`sys.stdout` 捕获改为在进程入口幂等安装（不再由后台
  线程改进程级全局），并补齐文本流协议、加锁与防重入（避免日志自我喂入
  死锁）；未捕获异常钩子改为「先写 crash.log、再调用原钩子」，不再静默
  吞掉标准回溯。

### 其他

- README 补充「第三方依赖与致谢」：声明 PyQt5 / pywin32 / Pillow / pyzbar /
  numpy / opencv-python / 7-Zip 等依赖及各自许可（代码本体为原创，MIT）

## [1.0.1] - 2026-09-06

### 新增

- **拖放临时解压**：把文件拖入程序主界面即可解压，**无需在监控路径下**。
  - 支持单个或多个文件；解压结果输出到**文件所在目录**（自动建同名文件夹）
  - 只处理拖入的文件；同目录其他文件不处理（除非是它自己的分卷兄弟）
  - 完整智能解压：多层穿透、密码尝试、伪装格式识别
  - 分卷处理：拖入首卷（如 `.001`）自动归拢同目录所有分卷统一解压；
    拖入非首卷（`.002`）提示「请拖入首卷」；伪装分卷名的完整包正常处理
  - 拖放不删除源文件；拖入非压缩包/下载未完成的文件会提示跳过
  - 后台线程执行，不阻塞界面

- **自动更新**（设置 → 常规 → 版本与更新）：
  - 检测到新版本后，「前往下载更新」按钮可点击，执行完整自动更新流程：
    下载新版 → 校验压缩包 → 生成更新脚本 → 结束当前进程 → 备份旧代码 →
    覆盖新代码 → 自动重启
  - 更新过程**不弹窗**，进度显示在设置页文本行
  - 用户数据（config.json / toolbox.db / temp_passwords.json /
    deletion_trail.json / logs / backup）**绝不覆盖**（robocopy 显式排除 +
    代码层白名单双保险）
  - 更新前旧代码自动备份到 `backup\update_before_<时间戳>\`，可回滚
  - 下载失败自动重试 3 次（GitHub 偶发 502/超时，国内网络更常见）

- **版本与更新检查**（设置 → 常规）：
  - 显示当前版本号；「检查更新」按钮**仅在用户手动点击时**才联网查询
    GitHub Releases API（绝不主动拉取）
  - 检测到有新版本时启用「前往下载更新」按钮；已是新版本则提示已是最新
  - 网络失败（如国内无法访问 GitHub）不弹窗，仅在下方显示一行提示
    「无法连接 GitHub，请检查网络后重试」
  - 检查在后台线程执行，不阻塞界面

### 修复

- **大文件伪装格式漏检**：`is_archive_file` 只看扩展名，导致图片/视频头 + 内嵌压缩包
  的伪装文件（如 JPEG 头 + 加密 7z，签名在文件前部而非尾部）被跳过不处理。
  - `is_archive_file` 现在对常见伪装载体（图片/视频/音频/文档扩展名）做深层格式探测
  - 新增前部扫描：大文件（>64MB 跳过全量扫描时）改扫文件前 32MB 找压缩包签名，
    覆盖「媒体头 + 紧随压缩包数据」的伪装结构
  - 实测约 1GB 的 JPEG 头 + 7z 加密伪装文件，修复后自动识别并解压成功

## [1.0.0] - 2026-09-05

### 首个正式版

首个正式版发布。项目从扁平脚本重构为 `autounpacker` 包结构，
并正式命名为 **AutoUnpacker**。

### 核心功能

- **多路径监听**：轮询指定目录（如百度网盘/IDM 下载目录），新压缩包出现即自动解压
- **智能解压**：自动识别真实格式（magic bytes，识别改名后缀迷惑的伪装压缩包）
- **多层穿透**：压缩包内嵌套压缩包自动递归解压到最内层成品
- **密码管理**：
  - 长期密码本（SQLite `toolbox.db`）
  - 临时密码本（本次开机内有效，剪贴板自动捕获，跨程序重启保留）
  - 密码字典（带使用次数统计）
  - 嵌套层自动沿用上一层成功的密码
- **分卷支持**：`.7z.001/.002`、`.part1.rar`、`.z01/.002` 等
  - 自动归拢散落在子目录的分卷兄弟到首卷旁边
  - 首卷按大小判断是否到齐，未到齐自动延迟等待
  - 嵌套分卷缺卷时整体判失败待重试（不把半成品当成品）
- **下载保护**：文件被占用（IDM 多线程合并/百度网盘写入中）自动 defer 等待
- **剪贴板监控**：二维码识别（子进程隔离）、短文本临时密码捕获、提取码回写
- **网址信任门卫**：二维码/剪贴板 URL 自动访问与打开前的信任判定
  （用户黑白名单 + 内置私网/回环/链路本地拦截，防 SSRF）
- **删除回溯**：解压完成后源文件移入回收站（可一键还原），全程可追溯
- **7-Zip 管理**：版本检测、隔离版安装（`%APPDATA%\AutoUnpacker\7z`）、升级引导
- **系统集成**：全局快捷键唤起、系统托盘、单实例检测、`--autostart` 后台启动

### 修复

- **Qt 平台插件找不到（venv 闪退）**：启动时自动定位并注入
  `QT_QPA_PLATFORM_PLUGIN_PATH`，venv 部署不再闪退
- **venv 下 pythonw 启动不稳定**：修复 Qt 插件路径解析导致窗口不出现/启动极慢
- **单实例检测静默退出**：被拦截时写日志提示，新增 `--force` 强制新开
  （清理僵尸实例）
- **7z 输出中文乱码**：7z 输出按 GBK 优先解码，修复 "正在使用此文件" 等
  错误串匹配失效导致的重试逻辑失效
- **嵌套分卷错误吞掉**：分卷缺卷报 "Unexpected end of archive" 时整体判失败，
  不再误报成功
- **密码错取**：嵌套层机械按层序取密码改为优先沿用上一层成功密码

### 性能

- **启动加速**：cv2/numpy/PIL/win32clipboard 惰性导入（只在用到时加载），
  QR 解码子进程化，主进程启动不再加载重依赖

### 迁移与兼容

- 配置向后兼容：新增配置键自动用默认值补齐，旧版本配置升级不崩
- 旧版扁平结构（`smart_extract.py` 等）自动迁移到包结构
- 数据格式（config/toolbox.db/temp_passwords/deletion_trail）与旧版完全兼容
- 配置/临时密码原子写入（临时文件 + `os.replace`），崩溃不留半截 JSON
- 配置文件损坏时自动备份为 `.corrupt` 再回退默认，不静默丢失用户配置

### 安全性

- 敏感数据（config.json、toolbox.db、temp_passwords.json、deletion_trail.json、
  logs/、backup/）全部列入 `.gitignore`，开源推送零泄漏
- 网址访问内置私网/回环地址拦截（默认开启），防 SSRF
- 密码经 7-Zip stdin 传输（7-Zip ≥ 18.00），避免命令行参数被任务管理器窥探
- 原生库（cv2/pyzbar/PIL）在独立子进程运行，崩溃不拖垮主程序

### 其他

- 项目正式命名 **AutoUnpacker**（原「智能工具箱」）
- 目录结构重构：`main.py`（薄入口）+ `autounpacker/` 包
- 新增 README.md、本 CHANGELOG.md