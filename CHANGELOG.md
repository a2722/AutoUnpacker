# Changelog

本项目所有重要变更都记录在此文件。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

- **监听路径新增「模式」：表层（安全）/ 百度清单（含子目录）**。表层模式即原有行为
  （只处理监听目录表面一层的文件）；百度清单模式会**额外**按百度网盘任务清单，处理
  下载到子目录里的压缩包与分卷（含归档内部穿透），**清单外的路径一律不碰**。
  两种模式都不新增监听路径，从结构上避免「同目录被两个监听器抢文件」。
  - 安全兜底：未开启实验性功能 / 客户端未运行 / 历史被清 → 自动只走表层模式；
  - 只处理「任务已完成」或「此前确实处理过（粘性记忆，存 `toolbox.db`）」的路径；
  - 文件大小需连续稳定 6 秒、且不是 `.baiduyun.p.downloading` 才处理；
  - **疑似程序目录保护**：同目录含 `.exe/.dll/.bat/...` 时只记一条日志、不自动解压；
  - 「解压到」目录与监听根重叠时提示一次，避免自我循环。
- 主界面「网盘下载目录」按钮：识别到的百度下载目录会以**百度清单模式**加入监听。
- **实验性：网盘任务跟踪（下载完成即触发 / 批次预登记 / 未完成提示 / 重复检测）**：
  - **批次预登记**：下载一开始就按「分享根」归批，输出整批期望文件清单（含套层的
    `local_path`），为提前建目录、提前判断分卷齐全提供权威输入；
  - **下载完成即触发**：任务从 `download_file` 消失且能在下载历史中确认时判定完成；
    分卷组全部完成且文件落盘后，`monitors` 的「等分卷」逻辑会**提前放行**解压
    （只加速、不阻断：拿不到信息一律回退原有磁盘启发式）；
  - **未完成任务提示**：启动时若 `download_file` 非空，通知「有 N 个网盘任务未完成
    （或仍在下载）」；
  - **重复下载检测**：新任务若与历史记录「同 `server_path` + 同 `size`」，可选提示。
- 通知页新增「网盘任务（实验性）」分组与三个独立开关（完成 / 未完成 / 重复）。
- **网址信任按用途拆成两套（互不影响）**：「自动在浏览器打开」（二维码解出的链接）
  与「下载识别二维码」（复制的网址被拉取、判断是否二维码图片）此前共用同一套
  `new_domain_action` + 白/黑名单，导致「同一个域名既想自动打开、又不想被扫描」
  无法两全（加黑名单会连带禁掉自动打开）。现 `url_trust` 拆为 `open` / `fetch`
  两份独立配置，各有自己的新域名默认行为与白/黑名单；内置敏感地址拦截仍两用途共享。
  设置页「网址信任」相应拆成两个分区；询问弹窗的「永久信任 / 永久拒绝」按当前用途
  写入对应名单。**旧版扁平配置启动时自动迁移到两边**，行为不变。
  （例：网盘分享链接不可能是二维码，可把 `drive.uc.cn` 放入 `fetch` 黑名单、
  同时留在 `open` 白名单里自动打开。）
- **实验性（2.F）：分享「链接 ↔ 客户端下载任务」关联打通（链接→提取码→下载→解压）**。
  客户端分享下载时，本地库活动表 `download_file.download_url` / `param2` 里带
  `shareid` / `share_uk` / `fs_id` / `md5` / `sekey`（只在**下载进行中**才有值，完成进
  历史表就没了）；而分享页 `https://pan.baidu.com/s/<surl>` **公开可读、无需登录**，
  `window.yunData` 里有同组 `shareid` / `share_uk`。现在：
  - `baidu_db` 活动任务查询新增读 `download_url` / `param2` / `error_code` / `status_changetime`；
  - `baidu_manifest` 新增纯解析 `parse_share_download()`（从 download_url/param2 取
    shareid/share_uk/fs_id/md5/sekey）、`extract_share_ids_from_html()`（从分享页 HTML 取
    shareid/share_uk）、`parse_share_url()`、`remember_share_link()`、`share_link_for()`；
  - 剪贴板出现百度分享链接时（`qr_url_enabled`）顺手抓页并记住 `surl → shareid`；
  - `observe_tasks` 给活动任务挂 `share` 关联键，新任务日志据此输出
    「分享 shareid=… uk=… fs_id=…，链接 …」。
  于是「链接 ↔ 下载任务」可用 `shareid` 精确对齐（兜底：`server_path`+`size`+`md5`）。
  - 另新增 `baidu_share.invoke_download()`：完整复刻「分享链接 → tplconfig 取 sign →
    verify 校验提取码得 sekey → share/list 列文件 → sharedownload 转存（拿到不透明的
    `filelist` 字符串令牌）→ invoker/get + online + send 投递 → 用
    `baiduyunguanjia://evoked-download/?browserId=…&seq=…` 唤起客户端 → invoker/check
    轮询确认」这条链路，即可**用客户端下载该分享（"拉起"下载）**——不下载、不登录、
    不碰网页，绕开 IDM。

### 修复

- **「设置 → 常规 → 主题」下拉的展开方式三套主题不一致**（浅色向下带动画、跟随
  系统向下瞬时、深色向上瞬时）：根因是 QComboBox 弹出列表的**方向 / 动画 /
  默认委托**都由同一个 style hint `SH_ComboBox_Popup` 决定——浅色 `windowsvista`
  返回 0（下拉式：在组合框下方展开 + 150ms 滚动动画），深色 `Fusion` 返回 1
  （菜单式：相对当前项定位、可能向上弹、且不播放动画）。现用 `QProxyStyle` 把
  深色样式的该 hint 也归零，三套主题统一为「在下方展开 + 展开动画」。
- **浅色主题下主题下拉展开后高度塌陷**：切换到「浅色」后再次点开主题下拉，弹出的
  选项列表只剩约一行字高，三个选项挤在一起、文字被裁；而「跟随系统 / 深色」两项
  正常。根因是下拉列表的默认委托 `QComboBoxDelegate`（继承 `QItemDelegate`）
  **不识别 QSS 的 `QComboBox QAbstractItemView::item` 行高规则**，行高于是退回
  字高（约 13px）。现：给弹出项补上 `padding` 行高规则，并在控件 polish / show 时
  把下拉列表委托统一换成 `QStyledItemDelegate`（应用级事件过滤器，可覆盖运行期才
  创建的对话框控件）；配合上一条把两套样式都归一为下拉式，两套主题的弹出高度与
  行高保持一致（浅色约 23px/行、深色 21px/行）。
- **深色主题下勾选框 / 单选框看不见**：深色下复选框的方框、单选框的圆圈几乎与
  背景同色（只剩对勾 / 圆点悬空可见）。根因是样式表给 `QCheckBox / QRadioButton`
  设了 `background: transparent`，`QStyleSheetStyle` 会连带把绘制指示器所用调色板
  的 `Base` 变成透明 / 黑，Fusion 便用「≈背景」的颜色画框；浅色用原生
  `windowsvista` 绘制指示器，不受影响。现：把 `QCheckBox / QRadioButton` 从
  transparent 规则中移除，给「未选中」态补一条主题配色的可见描边；「已选中」态
  改为**主题主色填充 + 白色对勾 / 圆点**，一并解决「选中态反而不如未选中醒目」。
- **所有主题下组合框都没有下拉箭头**：只要用 QSS 定制了 `QComboBox::drop-down`
  （本样式表为了扁平外观设了 `border: none; width: 22px`），Qt 就不再自动绘制
  `::down-arrow`——浅色 / 深色都一样；而 QSS 的 `::down-arrow` 只认 `image:`
  （CSS 三角写法在子控件上不生效）。现：运行时用 QPainter 生成主题配色的 V 形箭头
  PNG，再以 `url()` 引回样式表，既保留扁平外观，箭头也随主题取色。
- **运行时生成的主题图标散落在数据目录**：下拉箭头 / 选中对勾 / 单选圆点的 PNG
  原先直接写在数据目录（且以 `.` 开头），现统一移入 `cache/` 子目录（`paths.CACHE_DIR`），
  并把 `.gitignore` 由「逐个文件名」改为忽略整个 `cache/`。
- **实验性「百度清单模式」：同一文件被重新下载后不会被解压**。根因在任务跟踪状态机
  `observe_tasks()`：它只在**首次**看到某个 `local_path` 时记录 `task_id`；重新下载
  同一路径时复用了旧条目却不刷新 `task_id`，于是新任务结束时用旧 `task_id` 去匹配
  「消失的任务」永远匹配不上，该条目的状态就**永远停在 `active`**——而 `_baidu_poll`
  只处理 `state=="done"` 的条目，于是永不解压。现：① 重新下载（同路径再次出现）时
  刷新 `task_id` / `server_path` / `size` 等字段；② 完成判定不再依赖相邻两拍的差集，
  改为「该条目是否仍在当前 `download_file` 活动集合里」，与 `task_id` 是否陈旧无关。
- **下载目录推断在历史只有一条记录时把「根」推成文件路径**：`detect_download_root()`
  用 `os.path.commonpath()` 求公共前缀，单条记录会直接返回该文件本身，使「下载根」
  变成一个文件路径（影响主界面「网盘下载目录」按钮与日志提示）。现改为对**文件所在
  目录**求公共前缀，保证结果一定是目录。
- 开启实验性功能、但识别出的百度下载目录**未加入监听**时，日志补一句提示
  「该目录仅监控、不解压」，避免误以为「开了实验性 = 来料会被自动处理」
  （监听范围始终是显式白名单）。
- **百度清单模式：解压产物被抬升到监听根，破坏下载目录结构**。`_handle()` 里
  `promote_to` 默认取的是**监听路径根**，解压成功后把顶层文件夹「抬升」到根目录；
  表层模式下源文件本就在根、看不出来，但百度清单模式处理的是下载到**子目录**里的
  包，于是产物跑到根目录、原位留下空文件夹，也断开了「文件取自哪个下载目录」的关系。
  现默认改为**源文件所在目录**（表层模式下二者等价，子目录场景则保留原结构）。
- **实验性「百度清单模式」：同一套分卷被下载到不同子目录时无法解压**。有些分享把
  一套分卷分散在多个子目录（`E:\test\假如说\1\test.7z.001`、`…\2\test.7z.002`），而
  7-Zip 只在同一目录里找兄弟卷——既有到齐判定（`_volume_ready` / `volume_hint` /
  `_stage_rar_volumes`）都是**目录局部**的，于是首卷一直判「未到齐」，等到超时才硬试、
  7-Zip 报缺卷，反复失败。现：清单侧新增 `gather_volume_set()`（同批次 + `is_volume_file`
  口径识别同套分卷、**卷号重复即判定歧义并放弃**），`_baidu_poll` 在首卷为跨目录集合
  且清单显示全部下完时，先把散落的兄弟分卷**归拢**到首卷目录（同盘 `shutil.move` 即
  瞬间改名，冲突/失败则回滚），再交回既有目录局部管线——到齐判断、解压、源文件回收
  （含分卷）与删除回溯全部复用，产物落在首卷目录。只凑清单内同批次同系列的分卷，
  清单外一律不碰。
- **剪贴板网址识别：把「链接 + 提取码」整段当网址访问，报 `URL can't contain control
  characters`**。复制分享文本（如 `https://drive.uc.cn/s/650826bca4d74 码：KKu7`）时，
  「网址形式的二维码图片识别」（`qr_url_enabled`）会把整段文本直接交给 urllib 发请求，
  其中的空格/中文触发 `URL can't contain control characters. '/s/650826... 码：KKu7'`
  而被跳过（日志「网址访问失败（跳过）」）。现在 `_extract_url()` 按 RFC 3986 的 ASCII
  URL 字符集，在**首个非法字符（空格/中文/全角标点）处截断**再返回，`_maybe_process_url()`
  只访问截出的真正网址；顺带修好了「网址后紧跟中文、中间无空格」的变体。

- **跨来源分卷：同一套分卷被拆进多个源文件时，既解不出、又被误报「完成」**。上传者把
  一套分卷切开塞进两个不同的伪装 `.mp4`，程序按源文件各自解到各自目录：末卷 `xxx.zip`
  报 `Missing volume : xxx.z01`、首卷 `xxx.z01` 单独留下，两半永不碰面。两个毛病：
  ① `extract.py` 判「嵌套缺卷」只看 `is_volume_name`（`.NNN`/`.zNN`/`.partN.rar`），
  而末卷 `base.zip`/`base.rar` 不带编号 → 落进「损坏可跳过」分支，保留失败文件后**整体
  仍报成功**（假成功）；② 到齐判断只查同一目录。现 `extract.py` 新增
  `is_split_gap_error()`（`"Missing volume"` 本身即判缺卷），缺卷时**整体判失败**并保留
  锚点分卷（`keep_output_dir`，失败回退不再清掉分卷，且不再把已就位的分卷误改名为
  `_failed`）；`monitors.py` 新增跨目录归拢：在监听范围（监听根 + 输出目录）内按系列名
  找回兄弟分卷、搬到锚点旁重试，等待期间不重复解压（`_split_pending` + 10s 节流复查），
  凑齐即恢复并回收已消费的源文件；超时（30 分钟）放弃且**分卷与源文件都保留**，可手动处理。

## [1.0.3] - 2026-09-12

### 新增

- **实验性功能开关（默认关闭）**：`设置 → 常规 → 「开启实验性功能」`。开启后，
  程序会以**只读**方式探测百度网盘客户端的本地任务库
  （`…\users\*\BaiduYunGuanjia.db`），并在启动时向运行日志输出「下载批次 /
  分卷组」汇总，用于验证按批次还原目录结构与分卷配对。安全约束：只读打开
  （`mode=ro`）+ 短连接（打开→查询→立即关闭）+ 本端 `busy_timeout` + 异常全部
  回退；不写库、不改 journal_mode、不 VACUUM、不持长事务，不影响正在运行的
  网盘客户端；且**绝不接触**运行中的 WAL 库 `BaiduYunCacheFileV0.db`。
- **实验性：百度网盘下载目录识别**：主界面新增「网盘下载目录」按钮（需先开启
  实验性功能）——只读任务库的历史 `local_path` 推断下载根目录（本机为 `E:\test`）
  并一键加入监听路径；已在监听中则提示。
- **实验性：活动下载任务轮询**：后台低频（5s）只读 `download_file`，下载开始/结束
  时在运行日志输出活动任务清单（目标 `local_path`、大小），用于验证"下载即预登记"
  （文件落盘前就掌握批次/结构/分卷）。

### 修复

- **无扩展名压缩包解压后源文件不删、残留 `*_extracted` 中间目录**：当压缩包
  没有扩展名（如 `E:\test\2022`）且其内层顶层文件夹同名（`2022`）时，提升逻辑
  的目标恰好是源文件本身；`_dirs_conflict` 把「目标是文件」误判为无冲突，进而
  尝试把文件夹「合并」进一个文件路径而抛异常，异常被后处理静默吞掉，导致
  **源文件与输出目录都没被回收**。现：合并前先确认目标是目录；若占位的正是本次
  要回收的源文件/其分卷，则先回收让出位置，提升后文件夹用回原名（不再退化成
  `名称(1)`），且删除回调只触发一次（回收/永久删除合并上报）。
- **设置页首次打开被压扁/文字被裁**：设置窗口初始尺寸写死 `640×520`，
  加了「网址信任」第 4 个单选项等之后，长页高度超出，导致首次打开控件被压缩、
  文字看不清，要拖动窗口才撑开。现改为按内容自适应初始尺寸，并把最小高度抬到
  内容下限（`minimumSizeHint`），长页首屏即完整显示。
- **长选项文案过长**：解压/网址信任/二维码与剪贴板/通知/常规/密码本等处描述过长的
  选项文案一律缩短，原详细说明移入**悬停提示**（与「临时密码」两项一致），
  避免低分辨率或默认宽度下文字被截断。

## [1.0.2] - 2026-09-11

### 新增

- **二维码内容分类处理**：解码出的文本按内容类型区分处理（纯扫描二维码
  与「复制链接→识别二维码」两条路径统一复用同一套逻辑）
  - 无链接的普通内容 / 单个二维码出现多个链接 → 写回剪贴板；`code` 设置下
    内容含提取码则只抬升提取码
  - 纯链接 → 信任判定后打开浏览器，按设置做剪贴板联动（含提取码抬升）
  - 链接夹杂其他内容 → 截取链接打开（走黑白名单，阻止时内容仍写剪贴板）
    + 按设置写剪贴板：`code` 且文本内含提取码时**抬升该提取码**
    （如「…链接 提取码：Zdjn」→ 剪贴板只留 `Zdjn`），否则写回全部内容
- **临时密码自动清理**：临时密码除「本次开机内有效」外，再叠加**有效期**
  （默认 24 小时）与**保留条数上限**（默认 200 条），超时/超量自动淘汰；
  两项均可在设置里自行调整。修复 Windows「快速启动」下每天关机不重置开机
  计时、导致临时密码长期堆积的问题。
- **临时密码捕获过滤（「设置 → 二维码与剪贴板 → 临时密码」）**：两级开关——
  - 「网址排除」（父，宽松，默认开）：带 :// 的网址不记为临时密码
  - 「智能过滤」（子，更严格，默认开；缩进、父关则禁用）：在网址排除基础上，
    再排除多行文本、文件路径/UNC、带常见扩展名的文件名、含句读标点的句子
  - 父开关关闭 → **照单全收**（连网址也收）
  - 有效期 / 保留上限同处该分组
- **密码本窗口**：长期密码区与临时密码区改为可拖动分隔条，可自行调节两区高度。
- **通知开关补全（「设置 → 通知」）**：新增 3 个**托盘提示**独立开关——「已最小化
  到托盘」「程序已在运行，已打开主界面」「有新的网址等待确认」。此前这三类托盘
  气泡只受通知总开关控制，现可各自单独关闭（总开关关闭时一并禁用）。
- **网址信任「无操作」策略（「设置 → 网址信任」）**：遇到未信任的新域名时新增
  「无操作」选项并**设为默认**——命中的新公网域名不打开、不询问、也不记录，
  静默跳过（适合只想手动白名单放行的用户）。

### 修复

- **「自动信任并打开 / 自动拒绝」不写名单**：`decide_host` 在 `auto_whitelist` /
  `auto_blacklist` 下只返回放行/拒绝，从不把域名写入白名单/黑名单，导致选「自动
  信任并打开」后白名单始终为空（同一域名每次都被当成新域名）。现命中 auto 策略
  时把新公网域名持久化进对应名单（弹窗点「信任 / 永久拒绝」的路径本来就正确）。
- **二维码文本乱码**：二维码解码子进程（`qr_worker`）的 stdout 在管道下默认
  用系统编码（中文 Windows 为 GBK）输出，父进程却按 UTF-8 解码，导致二维码里的
  中文（含「提取码」字样）变成乱码，只有 URL、提取码等 ASCII 幸存（所以链接仍能
  正常打开）。现强制子进程以 UTF-8 输出，与父进程解码对齐，混合内容里的提取码
  才能被正确识别并按设置抬升。
- **分卷「批次括号」命名**（如外层 `…part01(2).rar` → 中层 `…part01(1).rar`
  → 内层 `…part01.rar`，基础名相同）：修正分卷兄弟判定、首卷识别与嵌套层
  规范化暂存，避免把不同批次的卷误当兄弟而误删下一层源卷，或首卷不入队
  导致嵌套文件被清理丢失。
- **0 字节幽灵文件**：文件占用检测不再用会「创建文件」的 `open(path,"ab")`
  探测（文件已被消费时会原地重建 0 字节空文件）；`is_archive_file` 对不
  存在的文件返回 False。
- **进程内全局状态**：`sys.stdout` 捕获改为在进程入口幂等安装（不再由后台
  线程改进程级全局），并补齐文本流协议、加锁与防重入（避免日志自我喂入
  死锁）；未捕获异常钩子改为「先写 crash.log、再调用原钩子」，不再静默
  吞掉标准回溯。

### 其他

- README 补充「第三方依赖与致谢」：声明 PyQt5 / pywin32 / Pillow / pyzbar /
  numpy / opencv-python / 7-Zip 等依赖及各自许可（代码本体为原创，MIT）

## [1.0.1] - 2026-09-06

### 新增

- **拖放临时解压**：把文件拖入程序主界面即可解压，**无需在监控路径下**。
  - 支持单个或多个文件；解压结果输出到**文件所在目录**（自动建同名文件夹）
  - 只处理拖入的文件；同目录其他文件不处理（除非是它自己的分卷兄弟）
  - 完整智能解压：多层穿透、密码尝试、伪装格式识别
  - 分卷处理：拖入首卷（如 `.001`）自动归拢同目录所有分卷统一解压；
    拖入非首卷（`.002`）提示「请拖入首卷」；伪装分卷名的完整包正常处理
  - 拖放不删除源文件；拖入非压缩包/下载未完成的文件会提示跳过
  - 后台线程执行，不阻塞界面

- **自动更新**（设置 → 常规 → 版本与更新）：
  - 检测到新版本后，「前往下载更新」按钮可点击，执行完整自动更新流程：
    下载新版 → 校验压缩包 → 生成更新脚本 → 结束当前进程 → 备份旧代码 →
    覆盖新代码 → 自动重启
  - 更新过程**不弹窗**，进度显示在设置页文本行
  - 用户数据（config.json / toolbox.db / temp_passwords.json /
    deletion_trail.json / logs / backup）**绝不覆盖**（robocopy 显式排除 +
    代码层白名单双保险）
  - 更新前旧代码自动备份到 `backup\update_before_<时间戳>\`，可回滚
  - 下载失败自动重试 3 次（GitHub 偶发 502/超时，国内网络更常见）

- **版本与更新检查**（设置 → 常规）：
  - 显示当前版本号；「检查更新」按钮**仅在用户手动点击时**才联网查询
    GitHub Releases API（绝不主动拉取）
  - 检测到有新版本时启用「前往下载更新」按钮；已是新版本则提示已是最新
  - 网络失败（如国内无法访问 GitHub）不弹窗，仅在下方显示一行提示
    「无法连接 GitHub，请检查网络后重试」
  - 检查在后台线程执行，不阻塞界面

### 修复

- **大文件伪装格式漏检**：`is_archive_file` 只看扩展名，导致图片/视频头 + 内嵌压缩包
  的伪装文件（如 JPEG 头 + 加密 7z，签名在文件前部而非尾部）被跳过不处理。
  - `is_archive_file` 现在对常见伪装载体（图片/视频/音频/文档扩展名）做深层格式探测
  - 新增前部扫描：大文件（>64MB 跳过全量扫描时）改扫文件前 32MB 找压缩包签名，
    覆盖「媒体头 + 紧随压缩包数据」的伪装结构
  - 实测约 1GB 的 JPEG 头 + 7z 加密伪装文件，修复后自动识别并解压成功

## [1.0.0] - 2026-09-05

### 首个正式版

首个正式版发布。项目从扁平脚本重构为 `autounpacker` 包结构，
并正式命名为 **AutoUnpacker**。

### 核心功能

- **多路径监听**：轮询指定目录（如百度网盘/IDM 下载目录），新压缩包出现即自动解压
- **智能解压**：自动识别真实格式（magic bytes，识别改名后缀迷惑的伪装压缩包）
- **多层穿透**：压缩包内嵌套压缩包自动递归解压到最内层成品
- **密码管理**：
  - 长期密码本（SQLite `toolbox.db`）
  - 临时密码本（本次开机内有效，剪贴板自动捕获，跨程序重启保留）
  - 密码字典（带使用次数统计）
  - 嵌套层自动沿用上一层成功的密码
- **分卷支持**：`.7z.001/.002`、`.part1.rar`、`.z01/.002` 等
  - 自动归拢散落在子目录的分卷兄弟到首卷旁边
  - 首卷按大小判断是否到齐，未到齐自动延迟等待
  - 嵌套分卷缺卷时整体判失败待重试（不把半成品当成品）
- **下载保护**：文件被占用（IDM 多线程合并/百度网盘写入中）自动 defer 等待
- **剪贴板监控**：二维码识别（子进程隔离）、短文本临时密码捕获、提取码回写
- **网址信任门卫**：二维码/剪贴板 URL 自动访问与打开前的信任判定
  （用户黑白名单 + 内置私网/回环/链路本地拦截，防 SSRF）
- **删除回溯**：解压完成后源文件移入回收站（可一键还原），全程可追溯
- **7-Zip 管理**：版本检测、隔离版安装（`%APPDATA%\AutoUnpacker\7z`）、升级引导
- **系统集成**：全局快捷键唤起、系统托盘、单实例检测、`--autostart` 后台启动

### 修复

- **Qt 平台插件找不到（venv 闪退）**：启动时自动定位并注入
  `QT_QPA_PLATFORM_PLUGIN_PATH`，venv 部署不再闪退
- **venv 下 pythonw 启动不稳定**：修复 Qt 插件路径解析导致窗口不出现/启动极慢
- **单实例检测静默退出**：被拦截时写日志提示，新增 `--force` 强制新开
  （清理僵尸实例）
- **7z 输出中文乱码**：7z 输出按 GBK 优先解码，修复 "正在使用此文件" 等
  错误串匹配失效导致的重试逻辑失效
- **嵌套分卷错误吞掉**：分卷缺卷报 "Unexpected end of archive" 时整体判失败，
  不再误报成功
- **密码错取**：嵌套层机械按层序取密码改为优先沿用上一层成功密码

### 性能

- **启动加速**：cv2/numpy/PIL/win32clipboard 惰性导入（只在用到时加载），
  QR 解码子进程化，主进程启动不再加载重依赖

### 迁移与兼容

- 配置向后兼容：新增配置键自动用默认值补齐，旧版本配置升级不崩
- 旧版扁平结构（`smart_extract.py` 等）自动迁移到包结构
- 数据格式（config/toolbox.db/temp_passwords/deletion_trail）与旧版完全兼容
- 配置/临时密码原子写入（临时文件 + `os.replace`），崩溃不留半截 JSON
- 配置文件损坏时自动备份为 `.corrupt` 再回退默认，不静默丢失用户配置

### 安全性

- 敏感数据（config.json、toolbox.db、temp_passwords.json、deletion_trail.json、
  logs/、backup/）全部列入 `.gitignore`，开源推送零泄漏
- 网址访问内置私网/回环地址拦截（默认开启），防 SSRF
- 密码经 7-Zip stdin 传输（7-Zip ≥ 18.00），避免命令行参数被任务管理器窥探
- 原生库（cv2/pyzbar/PIL）在独立子进程运行，崩溃不拖垮主程序

### 其他

- 项目正式命名 **AutoUnpacker**（原「智能工具箱」）
- 目录结构重构：`main.py`（薄入口）+ `autounpacker/` 包
- 新增 README.md、本 CHANGELOG.md