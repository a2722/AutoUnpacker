# -*- coding: utf-8 -*-
"""autounpacker.baidu：百度网盘任务库（实验性，只读）的所有实现。

本文件只有模块说明，不导入任何子模块，保证
`import autounpacker.baidu` 零副作用。
子模块：
  db       只读访问层（选库、短连接查询、列容错、读活动/历史）
  manifest 批次/分卷还原 + 任务跟踪事件（纯逻辑、无线程）
  share    分享链接「拉起客户端下载」全链路
  task     门面：转发以上模块，保持旧路径可用
  watch    后台轮询线程 + 一次性诊断 + 启动探测
旧路径（包根 baidu_*.py）是兼容别名 shim，与本包对应模块是同一对象。
"""
